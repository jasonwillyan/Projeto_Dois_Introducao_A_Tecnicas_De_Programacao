#ifndef calcula_h ///< Estrutura de condição que verifica se arquivo .h já foi incluído
#define calcula_h ///< Faz a inserção caso o arquivo .h não tenha sido inserido 
#include "struct.h" ///< Inclusão do arquivo .h da struct

SIR* calcula(MEC *mec, SIR *sir, int *c);

#endif ///< Fim da estrutura de condição